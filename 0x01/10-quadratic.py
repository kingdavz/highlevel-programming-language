#!/usr/bin/python
"""A module that can solve quadratic equations"""

def solve_quadratic(a, b, c):
    if a == 0
    return "not a quadratic equation."

discriminant = b**2 - 4*a*c

if discriminant > 0:
    root1 = 
